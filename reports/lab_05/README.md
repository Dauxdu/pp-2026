# Лабораторная работа 5 — параллельный K-Means (Stanford CS149 asst1, Program 6)

Задание: взять любую программу из репозитория [stanford-cs149/asst1](https://github.com/stanford-cs149/asst1) и выполнить её по пунктам соответствующего задания. Выбрана программа **Program 6 «Making K-Means Faster»**: распараллелить ровно одну функцию многопоточной версии k-means, получить ускорение и оформить решение в формате задания «I measured … → X → I tried … → speedup».

## 1. Цель работы

Задача — профилировать многопоточную версию k-means (`kmeansThread.cpp`), выявить горячую функцию, распараллелить её без гонок данных, подтвердить совпадение результатов с оригиналом и достичь ускорения.

Ограничения задания: изменять можно только `kmeansThread.cpp`; распараллеливается ровно одна функция; остальные файлы сохранены без изменений.

## 2. Алгоритм и ход работы

### Шаг 1. Измерение

Три функции тела главного цикла — `computeAssignments`, `computeCentroids`, `computeCost` — обёрнуты вызовами `CycleTimer::currentSeconds()`, время просуммировано по всем итерациям до сходимости. Реальные замеры на данных генератора (M=5000, N=100, растёт K):

| K   | computeAssignments | computeCentroids | computeCost |
| --- | ------------------ | ---------------- | ----------- |
| 3   | 68.2 %             | 12.1 %           | 19.7 %      |
| 16  | 91.7 %             | 3.1 %            | 5.2 %       |
| 32  | 95.4 %             | 1.8 %            | 2.9 %       |

На больших объёмах доля горячей функции ещё выше: при M=100000, N=100, K=32 (54 итерации до сходимости) `computeAssignments` занимает 11.55 s из ≈12.1 s суммарно (95.2 %), `computeCentroids` — 2.2 %, `computeCost` — 2.6 %; при M=200000, K=64 доля достигает 97.1 %.

### Шаг 2. Вывод

`computeAssignments` — безусловная горячая точка (>90 % времени во всех конфигурациях с K ≥ 16). Это согласуется с анализом сложности: у неё O(K·M·N) (для каждой из M точек — расстояние до каждого из K центроидов), тогда как `computeCentroids` и `computeCost` — O(M·N); множитель K и делает её доминирующей. Значит, распараллеливать нужно именно её.

### Шаг 3. Что попробовал

Каждый поток берёт непересекающийся диапазон `m = [start, end)` и для каждой своей точки сам перебирает все K центроидов, выбирая ближайший. При такой схеме:

- `minDist` становится локальным скаляром потока — общего массива нет;
- каждый поток пишет только в свои ячейки `clusterAssignments` — гонок нет по построению, синхронизация не нужна вообще;
- M (~10^6) делится на любое число потоков с идеальной балансировкой (остаток раздаётся по одной точке первым потокам).

```cpp
// диапазон точек [start, end) закреплён за потоком
for (int m = start; m < end; ++m)
{
    float minDist = std::numeric_limits<float>::max();
    for (int k = 0; k < K; ++k)        // порядок k = 0..K-1 и строгое «<» как в оригинале
    {
        float d = dist(points[m], centroids[k]);
        if (d < minDist)
        {
            minDist = d;
            clusterAssignments[m] = k;
        }
    }
}
```

Порядок перебора `k = 0..K-1` со строгим `<` сохранён из оригинала, поэтому совпадают и результат, и правило разрешения ничьих. Число потоков — `std::thread::hardware_concurrency()`. Объём правки — около 25 строк плюс `#include <vector>`, как и ожидает задание.

### Шаг 4. Корректность

- Собраны оригинал и параллельная версия, прогнаны на одних данных: `end.log` идентичны (`diff` пуст), 0 расхождений в `clusterAssignments`, максимум 0.000e+00 по координатам центроидов — при 1..33 потоках и даже когда потоков больше, чем точек.
- `python3 plot.py` даёт осмысленную картину сходимости: три слипшихся стартовых центроида на `start.png` расходятся в три кластера на `end.png` (рисунки 1–4) — как требует задание.
- `dist`, `computeCentroids`, `computeCost`, `kMeansThread` побайтово как в оригинале; изменена только одна разрешённая функция.

## 3. Результаты

Итоговый замер на целевой машине (4 ядра / 8 hyper-threads), полный пайплайн до сходимости:

| Версия                                             | [Total Time], ms | Ускорение |
| -------------------------------------------------- | ---------------- | --------- |
| Оригинал (последовательный обход в `kmeansThread`) | 95900.688        | 1.00×     |
| Параллельная (`computeAssignments` по точкам)      | 32992.190        | **2.91×** |

Ускорение **2.91x** превышает порог задания 2.1x. По закону Амдала при доле горячей функции p ≈ 0.95 верхняя граница составляет ≈3.5x для 4 физических ядер и ≈5.9x для 8 hyper-threads; измеренное значение укладывается в эти границы, а отставание от границы объясняется стоимостью спавна потоков на каждой итерации и memory-bound характером вычисления расстояний.

## 4. Анализ графиков

<img width="1000" height="750" src="figures/start_original.png" alt="K-Means: Start, оригинальная версия" />
Рисунок 1 — K-Means: Start, оригинальная версия

<img width="1000" height="750" src="figures/end_original.png" alt="K-Means: End, оригинальная версия" />
Рисунок 2 — K-Means: End, оригинальная версия

<img width="1000" height="750" src="figures/start_parallel.png" alt="K-Means: Start, параллельная версия" />
Рисунок 3 — K-Means: Start, параллельная версия

<img width="1000" height="750" src="figures/end_parallel.png" alt="K-Means: End, параллельная версия" />
Рисунок 4 — K-Means: End, параллельная версия

На старте три начальных центроида (красные звёзды) находятся практически в одной точке в центре облака данных, поэтому начальная раскраска точек по ближайшему центроиду выглядит «перемешанной». После сходимости каждый центроид сместился в центр своего скопления, а точки разбились на три плотных кластера с небольшим перекрытием на границах. Пары рисунков оригинальной и параллельной версий воспроизводят одну и ту же структуру кластеров — визуальное подтверждение совпадения результатов.

## 5. Выводы

1. Профилирование выявило горячую точку — `computeAssignments` (>90 % времени при K ≥ 16, до 97.1 % на крупных K); именно она распараллелена, что совпадает с ограничением задания на одну функцию.
2. Разбиение работы по точкам, а не по центроидам, устранило гонки данных без какой-либо синхронизации: локальный `minDist`, непересекающиеся записи в `clusterAssignments`, идеальная балансировка нагрузки.
3. Корректность подтверждена бит-в-бит: `end.log` идентичны, расхождений нет при 1..33 потоках; графики `start.png`/`end.png` обеих версий совпадают по структуре кластеров.
4. Получено ускорение 2.91x (95900.688 ms → 32992.190 ms)
